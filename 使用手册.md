# EZ-Tree 程序化树木生成器 · 使用手册（中文版）

> 本文档对应本项目（`fuyunjian/ez-tree` fork）的中文界面版本。所有面板名称、控件标签均与页面上的中文界面一一对应；括号中的英文为对应的技术标识（保存的 JSON 预设、导出的文件与英文版文档中仍使用英文标识）。

---

## 目录

1. [项目简介](#1-项目简介)
2. [本地启动与构建](#2-本地启动与构建)
3. [页面布局总览](#3-页面布局总览)
4. [「树木」标签页 · 面板详解](#4-树木标签页--面板详解)
5. [古柏预设（一键应用）](#5-古柏预设一键应用)
6. [单枝精细编辑（点击选枝）](#6-单枝精细编辑点击选枝)
7. [「导出」标签页 · 保存与导出](#7-导出标签页--保存与导出)
8. [常见问题与故障排查](#8-常见问题与故障排查)

---

## 1. 项目简介

EZ-Tree 是一个基于 Three.js 的程序化树木生成库与在线编辑器：通过一组参数即可实时生成、预览并导出高保真 3D 树木模型（GLB / LOD 包 / PNG 截图）。

本 fork 在原版基础上进行了大幅扩展，重点解决「树木形状灵活性低」的问题，新增能力包括：

| 能力 | 说明 |
| --- | --- |
| **参数上限解除** | 枝干长度上限提升至 500、半径上限提升至 50、树叶数量上限提升至 1000，可生成大树冠覆盖。 |
| **单枝覆盖（Override）** | 在 3D 视口中点击任意一根枝干，即可单独调整它的长度、半径、角度、子枝数、蜿蜒度、锥度、扭转、分段数、起始位置，不影响其他枝干。 |
| **每枝独立随机（RNG Mode）** | `每枝独立 (Per Branch)` 模式下，每根枝干拥有独立的随机数流，修改单枝不会扰动兄弟枝干；`全局共享 (Shared, 旧版)` 保持原版行为。 |
| **弯曲控制点（Bend Points）** | 对选中枝干可添加任意数量的弯曲控制点，逐点控制位置（t）、方向（x/y/z）与强度，实现自定义曲线造型。 |
| **树干塑形（Trunk Sculpt）** | 基部膨大、弓弯（弯度/高度/方向）、树干扭转、表面噪声，用于塑造苍劲的古树主干。 |
| **整体姿态（Global Pose）** | 全局倾斜（X/Z）、扭转与不对称，一键让整棵树呈自然歪斜、迎风生长的姿态。 |
| **板根（Buttress / Roots）** | 根部棱纹翅板（数量/强度/高度/相位）+ 外露根手指（数量/长度/深度/宽度），还原热带/古树板根形态。 |
| **枯木与空洞（Deadwood）** | 树干空洞（强度/高度/宽度/相位）、纵向裂纹（数量/深度/宽度/相位）与随机枯枝（概率/长度），呈现沧桑感。 |
| **云片叶簇（Cloud Slab）** | 层叠的水平圆盘状叶簇，可复现古柏「云片」树冠；低细节 LOD 下自动退化为实心圆盘，性能友好。 |
| **古柏预设** | 一键按钮，组合以上全部特性生成「张飞柏」式古柏：膨大基部、弓弯扭转、板根、空洞裂纹、枯枝与云片树冠。 |

---

## 2. 本地启动与构建

### 2.1 安装依赖

```bash
# 本项目环境使用 bun 安装（npm 在本机 node_modules 非空时不稳定）
bun install
```

### 2.2 启动开发服务器

```bash
node node_modules/vite/bin/vite.js --config vite.app.config.js --port 5180
```

> **注意**：请直接使用 `node` 调用 vite 的入口脚本。本机 bun 1.3.14 的 `bunx vite` 封装会触发 Segmentation fault（已知回归），不要使用 `bunx vite`。

启动后浏览器访问 **http://localhost:5180** 即可。开发服务器带有热更新（HMR），修改 `src/` 下代码保存后页面自动刷新。

> 默认端口为 5180（避开本机其他项目占用的 5173）。若端口被占用，修改 `--port` 参数。

### 2.3 生产构建

```bash
# 构建到全新目录（本机安全钩子会拦截 vite 清空已存在的 outDir）
node node_modules/vite/bin/vite.js build --config vite.app.config.js --outDir dist-<名称>
```

### 2.4 技术栈

- Vite 6 + Three.js
- 核心库入口：`src/lib/`（`Tree`、`TreeOptions`、预设等，经 `@dgreenheck/ez-tree` 别名导出）
- 应用层：`src/app/`（`index.html`、`main.js` 入口、`ui.js` 控制面板、`scene.js` 场景、`textures.js` 纹理加载）

---

## 3. 页面布局总览

打开页面后，视口是 3D 树木场景；左侧为可折叠控制面板，顶部为统计 HUD。

### 3.1 顶部统计 HUD（视口左上角）

| 项目 | 含义 |
| --- | --- |
| 三角形 | 当前预览细节下网格的三角形总数（枝干 + 树叶） |
| 顶点 | 当前网格顶点总数 |
| 构建耗时 (ms) | 最近一次重新生成树木的耗时（毫秒） |

HUD 右侧是 **LOD 细节切换按钮**：`完整` / `LOD1` / `LOD2` / `LOD3` / `LOD4`。点击可预览对应细节等级下的网格（悬停按钮可看到该等级自动切换的视距）。切换仅影响预览，导出 GLB 始终使用完整细节。

### 3.2 视口操作

| 操作 | 效果 |
| --- | --- |
| 左键拖拽 | 旋转视角 |
| 右键 / 中键拖拽 | 平移视角 |
| 滚轮 | 缩放 |
| **单击枝干**（左键、位移小于 5px） | **选中该枝干**，左侧「选中枝干」面板展开，可单独编辑（见第 6 节） |
| 单击空白处 | 取消选中 |

> 页面背景会异步加载约 100 棵远景树（加载进度显示在启动画面）；远景树仅供氛围，不可选中。

### 3.3 控制面板

面板顶部为标题「EZ Tree / 程序化树木生成器」，下方是两个标签页：

- **树木**：全部生成参数（默认页）
- **导出**：预设保存/加载与模型导出

每个参数区块（Section）均可点击标题折叠/展开；区块内的小节（Subsection）同样可折叠。修改任意参数后树木会立即重新生成。

---

## 4. 「树木」标签页 · 面板详解

### 4.1 预设 (Presets)

| 控件 | 说明 |
| --- | --- |
| 预设 | 内置预设下拉：白蜡 小/中/大、白杨 小/中/大、橡树 小/中/大、松树 小/中/大、灌木 1/2/3、藤架（括号内为英文标识，保存的 JSON 中沿用） |
| 种子 | 随机数种子（0–65536）。固定种子可复现同一棵树 |
| 随机种子 | 一键取随机种子并重新生成 |

### 4.2 树皮 (Bark)

| 控件 | 说明 |
| --- | --- |
| 类型 | 树皮纹理（Bark001–Bark015，对应 `public/textures/bark/` 目录） |
| 色调 | 树皮整体颜色着色 |
| 平直着色 | 开启后每个三角面独立着色（低多边形风格） |
| 启用纹理 | 是否加载树皮贴图（关闭则使用纯色） |
| 纹理缩放 X / Y | 树皮贴图在水平/垂直方向的重复缩放 |

### 4.3 枝干 (Branches)

枝干分 4 个层级：`层级 0` 为树干，`层级 1–3` 为逐级分枝。

| 控件 | 说明 |
| --- | --- |
| 树木类型 | 落叶树 (Deciduous) / 常绿树 (Evergreen)：决定树叶的默认分布逻辑 |
| 层级数 | 分支层级总数（0–3） |
| 随机数模式 | `每枝独立 (Per Branch)`（推荐）：每根枝干独立随机，单枝修改不扰动他枝；`全局共享 (Shared, 旧版)`：沿用原版全局随机流 |

**子区（以下均为「层级 N」滑块，N=0–3，部分为 1–3）：**

| 子区 | 控件 | 范围 | 说明 |
| --- | --- | --- | --- |
| 角度 | 层级 1–3 | 0–180° | 子枝相对父枝的展开角度 |
| 子枝数 | 层级 0 / 1 / 2 | 0–100 / 1–25 / 2–20 | 每根枝干产生的子枝数量 |
| 蜿蜒度 | 层级 0–3 | -1 ~ 1 | 枝干随机弯曲程度（原版为单点随机，本 fork 支持配合「弯曲控制点」精确塑形） |
| 生长方向 | 方向 X / Y / Z | -1 ~ 1 | 对整层所有枝干施加的全局生长方向 |
| | 强度 | -0.1 ~ 0.1 | 生长方向力的强度 |
| 长度 | 层级 0–3 | 0.1 – 500 | 枝干长度（原版上限 100，已解除） |
| 半径 | 层级 0–3 | 0.1 – 50 | 枝干基部半径（原版上限 5，已解除） |
| 分段数 | 层级 0–3 | 1 – 40 | 枝干沿长度方向的纵向分段（越多越平滑，也越耗性能） |
| 径向段数 | 层级 0–3 | 3 – 16 | 枝干横截面的径向分段（越多越圆） |
| 起始位置 | 层级 1–3 | 0 – 1 | 子枝从父枝的哪个位置开始生长（0=基部，1=梢端） |
| 锥度 | 层级 0–3 | 0 – 1 | 枝干从基部到梢端的收窄程度 |
| 扭转 | 层级 0–3 | -0.5 ~ 0.5 | 枝干沿自身轴的扭转 |

### 4.4 树干塑形 (Trunk Sculpt)

作用于树干（层级 0）的形态雕刻，是塑造古树主干的核心。

| 控件 | 说明 |
| --- | --- |
| 基部膨大 | 1–3：树干基部加粗倍数（1=不膨大），用于表现古树根部隆起 |
| 膨大高度 | 0.05–1：膨大区占树干高度的比例 |
| 弓弯 | 0–20：主干整体弓形弯曲幅度 |
| 弓弯高度 | 0.1–0.9：弓弯顶点所在高度比例 |
| 弓弯方向 | 0–2π：弓弯朝向（弧度） |
| 扭转 | -2 ~ 2：主干沿纵向的螺旋扭转（表现千年古柏的螺旋纹理） |
| 表面噪声 | 0–2：主干表面随机起伏幅度 |
| 启用 | 总开关 |

**子区：板根 (Buttress / Roots)** —— 主干部位的棱纹翅板与外露根系：

| 控件 | 说明 |
| --- | --- |
| 启用 | 板根总开关 |
| 棱纹数 | 0–12：基部竖棱（翅板）数量 |
| 棱纹强度 | 0–1：棱纹凸起幅度 |
| 棱纹高度 | 0.05–1：棱纹向上延伸的高度比例 |
| 棱纹相位 | 0–2π：棱纹的周向起始角度 |
| 外露根数 | 0–16：基部向外伸出的根手指数量 |
| 根长度 | 1–20：外露根的长度 |
| 根深度 | 0–8：外露根扎入地下的深度 |
| 根宽度 | 0.1–1：外露根的粗细 |

**子区：枯木/空洞 (Deadwood)** —— 衰老特征：

| 控件 | 说明 |
| --- | --- |
| 启用 | 枯木总开关 |
| 空洞强度 | 0–1：主干表面局部凹陷（树洞）的深度 |
| 空洞高度 | 0.05–0.9：空洞中心所在高度比例 |
| 空洞宽度 | 0.05–1：空洞在周向的宽度（弧度比例） |
| 空洞相位 | 0–2π：空洞所在圆周方向 |
| 裂纹数量 | 0–12：纵向裂纹条数 |
| 裂纹深度 | 0–0.5：裂纹凹陷深度 |
| 裂纹宽度 | 0.01–0.3：裂纹在周向的宽度 |
| 裂纹相位 | 0–2π：裂纹的周向起始角度 |
| 枯枝概率 | 0–1：枝干变成枯枝（更短、更细、更扭曲、无叶）的概率 |
| 枯枝长度 | 0.2–1：枯枝相对正常枝干的长度比例 |

### 4.5 整体姿态 (Global Pose)

对整棵树施加的姿态变换（不影响各枝干的相对结构）：

| 控件 | 说明 |
| --- | --- |
| 倾斜 X / 倾斜 Z | -0.05 ~ 0.05：整棵树向 X / Z 方向倾斜（模拟风压、坡地生长） |
| 扭转 | -0.05 ~ 0.05：整棵树整体螺旋扭转 |
| 不对称 X / 不对称 Z | -0.05 ~ 0.05：左右/前后不对称生长 |
| 启用 | 总开关 |

> 提示：姿态数值为每单位长度的偏转系数，小数值即可产生明显效果，建议以 0.001 步长微调。

### 4.6 选中枝干 (Selected Branch)

未选中枝干时，面板显示提示「点击场景中的枝干，即可单独编辑它。」。选中后显示：

| 项目 | 说明 |
| --- | --- |
| 路径 | 该枝干的唯一路径 ID（如 `0`、`0.1`、`0c`），用于保存/加载后对应 |
| 长度 / 半径 / 角度 / 子枝数 / 蜿蜒度 / 锥度 / 扭转 / 分段数 / 起始 | 该枝干的**单枝覆盖值**，拖动即实时生效，不影响其他枝干 |

**弯曲控制点（Bend Points）：**

- 每个控制点包含：`#N 位置`（t，0–1，控制点在枝干上的位置）、`#N 方向 X/Y/Z`（-1~1，弯曲方向）、`#N 强度`（0–2，弯曲幅度）
- `添加弯曲控制点`：追加一个新的控制点（默认在 0.5 处向 +Z 弯）
- `移除控制点`：删除当前控制点
- `清除覆盖`：删除该枝干的全部覆盖与曲线设置，恢复由层级参数驱动

> 配合「随机数模式 = 每枝独立」，单枝编辑不会扰动相邻枝干，可放心逐根塑形。

### 4.7 树叶 (Leaves)

| 控件 | 说明 |
| --- | --- |
| 类型 | 白蜡 (Ash) / 白杨 (Aspen) / 橡树 (Oak) / 松树 (Pine)：叶面贴图 |
| 色调 | 树叶整体颜色 |
| 公告板 | 单片 (Single) / 双片 (Double)：树叶网格的交叉片数 |
| 角度 | 0–100：树叶与枝干的夹角倾向 |
| 数量 | 0–1000：单层树叶总数上限（原版上限已解除，大树可做满冠覆盖） |
| 最小层级 | 1–3：从第几层开始长叶（调低可让更多层级长叶，填满大树冠） |
| 密度 | 0–2：叶密度缩放（按枝干长度比例分配树叶） |
| 起始 | 0–1：枝干上开始长叶的位置（调大可让叶远离主干） |
| 大小 | 0–30：单片叶大小 |
| 大小变化 | 0–1：叶大小的随机变化幅度 |
| Alpha 测试 | 0–1：叶贴图透明裁剪阈值 |
| 圆滑法线 | 开启后树叶法线取球面法线，受光更柔和 |

**子区：云片叶簇 (Cloud Slab)** —— 古柏「云片」树冠专用：

| 控件 | 说明 |
| --- | --- |
| 启用 | 云片总开关（开启后在枝干末端生成层叠的水平圆盘叶簇） |
| 半径 | 1–20：单片云片的半径 |
| 厚度 | 0.2–5：云片总厚度 |
| 层数 | 1–5：云片叠放的层数 |
| 倾斜角 | 0–40°：云片的随机倾斜角 |
| 径向段数 | 6–20：云片的圆形分段数（越多越圆） |

> 与古柏预设配合：`数量` 调低、`最小层级` 调高、开启云片，即可得到「云片 + 稀疏针叶」的古柏树冠。

### 4.8 藤架 (Trellis)

| 控件 | 说明 |
| --- | --- |
| 启用 | 藤架总开关 |
| 可见 | 藤架网格是否可见 |
| 位置 X / Y / Z | 藤架的位置（X -20~20，Y -10~10，Z -20~20） |
| 宽度 / 高度 | 1–50：藤架平面尺寸 |
| 间距 | 0.5–10：格子间距 |
| 强度 | 0–0.2：藤架对枝干的吸附力 |
| 最大距离 | 0.5–20：吸附力作用范围 |
| 衰减 | 0.1–3：吸附力随距离的衰减指数 |
| 圆柱半径 | 0.01–0.5：藤架杆件粗细 |
| 颜色 | 藤架颜色 |

### 4.9 相机 (Camera)

| 控件 | 说明 |
| --- | --- |
| 自动旋转 | 视角自动环绕树木 |
| 旋转速度 | 0–2：自动旋转速度 |

### 4.10 环境 (Environment)

| 控件 | 说明 |
| --- | --- |
| 太阳角度 | 0–360°：天空球光照方向 |
| 草地数量 | 0–25000：地面草地实例数（越多越密，越耗性能） |

### 4.11 信息 (Info)

| 控件 | 说明 |
| --- | --- |
| 顶点数 / 三角形数 | 当前网格统计（与顶部 HUD 一致） |
| 版本 | 当前构建版本号 |
| 关于 | 打开关于弹窗（作者、联系方式、社交链接） |

---

## 5. 古柏预设（一键应用）

在「整体姿态 (Global Pose)」区块底部点击 **`古柏预设`** 按钮，一键将整棵树设为「张飞柏」式古柏造型：

- **基部膨大 1.8**，膨大高度 0.3（根部隆起）
- **弓弯 6**（高度 0.55、方向 0.4），树干扭转 0.25、表面噪声 0.8
- **板根启用**：棱纹 6 条、强度 0.4、外露根 7 条（长 7 / 深 2.2 / 宽 0.7）
- **枯木启用**：空洞（强度 0.35）+ 裂纹 4 条 + 枯枝概率 0.15
- **整体姿态**：轻微倾斜与扭转（不对称生长）
- 树干半径 8、长度 40、径向段数提升至 12（棱纹平滑）
- **云片叶簇开启**（半径 6、3 层）+ 树叶数量下调（满冠由云片承担）

点击后树木立即重建，所有相关控件同步刷新到新值。之后可继续微调各参数。

> 预设本身不会修改预设文件；如需保存当前参数，请使用「导出」页的「保存预设」。

---

## 6. 单枝精细编辑（点击选枝）

**操作流程：**

1. 在 3D 视口中**单击**想要编辑的枝干（注意：拖拽旋转视角不算点击）
2. 左侧「选中枝干」面板自动展开，显示该枝干的路径 ID（如 `0.1.2`）与 9 个覆盖滑块
3. 拖动滑块即可单独修改该枝干；树木实时重建
4. 如需曲线造型：点击「添加弯曲控制点」，逐点调整位置/方向/强度
5. 取消选择：点击视口空白处，或切换预设/加载预设（自动清除选中）
6. 恢复该枝干为层级默认：点击「清除覆盖」

**覆盖优先级：** 单枝覆盖 > 层级参数。被覆盖的属性以覆盖值为准，未覆盖的属性继续由「枝干 (Branches)」区块的层级参数驱动。

**随机数隔离：** 在 `每枝独立 (Per Branch)` 模式下，单枝的覆盖修改不会改变其他枝干生成结果；重新生成同一种子仍可复现（每枝随机流由 `路径ID + 种子` 决定）。

---

## 7. 「导出」标签页 · 保存与导出

### 7.1 保存与加载 (Save & Load)

| 按钮 | 说明 |
| --- | --- |
| 保存预设 | 将当前全部参数导出为 `tree.json`（含单枝覆盖、曲线点、板根、枯木、云片等全部配置） |
| 加载预设 | 选择之前保存的 `tree.json` 恢复参数并重建 |

> 加载后会清空当前选中的枝干，所有控件刷新为新参数。

### 7.2 导出模型 (Export Models)

| 按钮 | 说明 |
| --- | --- |
| 导出 GLB（完整细节） | 以**完整细节**导出当前树为 `tree.glb`（不受当前 LOD 预览等级影响），可拖入 Blender / 模型查看器 / 游戏引擎 |
| 导出 LOD 包 (ZIP) | 导出全部 LOD 等级（LOD0–LOD4）的 GLB，打包为 `tree_lods.zip`，命名 `tree_LOD0.glb` … `tree_LOD4.glb` |
| 导出 PNG | 导出当前视角的透明背景截图 `tree.png`（隐藏环境与草地，仅树木） |

> 导出期间按钮会显示进度文案（「正在导出 GLB…」「正在压缩…」等），完成后自动恢复。

---

## 8. 常见问题与故障排查

| 问题 | 原因与解决 |
| --- | --- |
| `bunx vite` 启动崩溃（Segmentation fault） | bun 1.3.14 在 Windows 上的已知回归。改用 `node node_modules/vite/bin/vite.js --config vite.app.config.js --port 5180` |
| `npm install` 中途失败/卡住 | 本机 npm 删除陈旧包时被安全钩子拦截。改用 `bun install`（已验证 925 包约 95s 完成） |
| `vite build` 报错无法清空输出目录 | 本机安全钩子拦截 vite 清空已存在 outDir。构建到**全新目录名**：`--outDir dist-<新名称>` |
| 端口被占用 | 默认 5180；如被占用，改 `--port` 参数重启 |
| 保存的 JSON 里是英文标识 | 属正常现象：JSON 与导出文件使用英文技术标识（如 `perBranch`、`trunk.buttress`），界面显示中文仅为展示层翻译 |
| 远景树太多、性能不佳 | 「环境」区块调低「草地数量」，或减少「枝干」区块的层级数/分段数/径向段数 |

---

*本手册随中文界面同步维护；若界面新增参数，请以页面实际控件为准。*
